{
  "name": "palank-mcp",
  "version": "3.2.0",
  "private": true,
  "type": "module",
  "description": "Palank domain MCP server — search_wiki / get_context / verify_before_tag (real implementations)",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "scripts": {
    "dev": "node server.js",
    "check": "node --check server.js"
  }
}
