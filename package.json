{
  "name": "palank-harness",
  "version": "3.3.1",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "description": "palank-harness v3.3 — echo-first transparent-wrapper interpreter on opencode",
  "license": "MIT",
  "repository": "github:PALAN-K/palank-harness",
  "keywords": [
    "opencode",
    "harness",
    "interpreter",
    "ai-agents",
    "llm",
    "developer-tools"
  ],
  "scripts": {
    "lint": "node --check plugins/force-delegation.js && node --check scripts/check_vault.js && node --check scripts/inventory.js && node --check scripts/validate-schema.js && node --check scripts/tiered-verify.js && node --check scripts/verify-tiered.js && node --check scripts/sync-version.js && node --check scripts/sync-architecture.js && node --check mcp/server.js",
    "check:vault": "node scripts/check_vault.js --strict",
    "test": "node --test \"tests/*.test.js\"",
    "inventory": "node scripts/inventory.js",
    "inventory:refresh": "node scripts/inventory.js --refresh",
    "sync:version": "node scripts/sync-version.js",
    "check:version": "node scripts/sync-version.js --check",
    "verify": "npm run lint && npm run check:vault && npm run test && npm run check:version && npm pack --dry-run",
    "verify:quick": "npm run lint && npm run check:vault -- --strict && npm test",
    "verify:tiered": "node scripts/verify-tiered.js",
    "tiered": "node scripts/tiered-verify.js --check",
    "sync:architecture": "node scripts/sync-architecture.js"
  },
  "files": [
    "AGENTS.md",
    "opencode.json",
    "index.md",
    "README.md",
    "skills/",
    "scripts/",
    "plugins/",
    "mcp/",
    "!mcp/node_modules",
    "tests/",
    "wiki/",
    "raw/",
    "log.md"
  ]
}
