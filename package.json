{
  "name": "palank-harness",
  "version": "3.2.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "description": "palank-harness v3.2 — echo-first transparent-wrapper interpreter on opencode",
  "scripts": {
    "lint": "node --check plugins/force-delegation.js && node --check scripts/check_vault.js && node --check scripts/inventory.js && node --check scripts/validate-schema.js && node --check mcp/server.js",
    "check:vault": "node scripts/check_vault.js --strict",
    "test": "node --test \"tests/*.test.js\"",
    "inventory": "node scripts/inventory.js --refresh",
    "sync:version": "node scripts/sync-version.js",
    "check:version": "node scripts/sync-version.js --check",
    "verify": "npm run lint && npm run check:vault && npm run test && npm run check:version && npm pack --dry-run"
  },
  "files": [
    "AGENTS.md",
    "index.md",
    "README.md",
    "skills/",
    "scripts/",
    "plugins/",
    "mcp/",
    "!mcp/node_modules",
    "wiki/",
    "raw/",
    "log.md"
  ]
}
